services:
  server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - cc_data:/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CCTI_DATA_DIR=/data
    restart: unless-stopped

volumes:
  cc_data:
